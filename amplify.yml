version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - nvm use 20
            - npm ci
        build:
          commands:
            - npm run build
        postBuild:
          commands:
            - echo 'Build completed successfully'
            - ls -la .next/
            - find .next -name "*manifest*" | head -5
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
        name: varekatalog-frontend
      cache:
        paths:
          - node_modules/**/*